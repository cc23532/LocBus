<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>LocBus Simulator</title>
    <!-- link css -->
    <link rel="stylesheet" href="../css/register.css" />


    <!-- font awesone cdn link -->
    <script
      src="https://kit.fontawesome.com/eeb24722a1.js"
      crossorigin="anonymous"
    ></script>
  </head>
  <body>
    <!-- Header -->
    <header>
      <a href="#" class="logo"
        ><img src="../assets/imgs/lockbus-logo.png" alt="Logo LocBus"
      /></a>
      <nav>
        <ul class="nav-links">
          <li><a href="/home">Início</a></li>
          <li><a href="/horarios">Horários</a></li>
          <li><a href="/consultaItinerarios">Itinerários</a></li>
          <li><a href="/consultaDados/<%= user.idUser %>" id="active">Dados do Usuário</a></li>
          <li><a href="/sobre-nos">Quem Somos</a></li>
          <li><a href="/suporte">Fale Conosco</a></li>
        </ul>
      </nav>
      <div class="suporte-icon">
        <a href="#"><i class="fa-solid fa-headset"></i></a>
      </div>
      </div>
    </header>

    <section class="section-form">
      <div class="form">
        <h1>ALTERAR DADOS</h1>
        <form action="/alterarDados" method="post">
          <div class="input-group">

            <div class="input-field">
              <i class="fa-solid fa-user"></i>
              <input type="text" value="<%= user.idUser %>" name="idUser" readonly>
            </div>
            
            <div class="input-field">
              <i class="fa-solid fa-user"></i>
              <input type="text" value="<%= user.nome %>" name="nome">
            </div>

            <div class="input-field">
              <i class="fa-solid fa-user"></i>
              <input type="text" value="<%= user.sobrenome %>" name="sobrenome">
            </div>

            <div class="input-field">
              <i class="fa-solid fa-address-card"></i>
              <input type="text" value="<%= user.cpf %>" name="cpf">
            </div>

            <div class="input-field">
              <i class="fa-solid fa-envelope"></i>
              <input type="email" value="<%= user.email %>" name="email">
            </div>

            <div class="field">
              <label for="linhaPreferida">Linha Preferida: </label>
            </div>

            <div class="input-field">
              <i class="fa-solid fa-bus-simple"></i>
              <select name="linhaPreferida" id="linhaPreferida" aria-placeholder="Linha favorita">
                <option value="675">675 - Campinas via Swift / Valinhos (Rodoviária)</option>
                <option value="676">676 - Campinas via Jd. do Lago / Valinhos (Jd. do Lago)</option>
                <option value="677">677 - Campinas (Rodoviária) / Valinhos (Pq. das Colinas)</option>
              </select>
            </div>
            <div class="btn-field">
              <button type="submit">Alterar</button>
            </div>
          </div>
        </form>
      </div>
    </section>
  </body>
</html>
